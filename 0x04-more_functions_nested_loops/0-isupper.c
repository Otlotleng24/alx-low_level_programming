#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @x: The number to be checked
 * Return: 1 if c uppercase
 */

int _isupper(int c)

{
	if (c >= 65  && c <= 90)
	{
	return (1);
	}
	return (0);
}
